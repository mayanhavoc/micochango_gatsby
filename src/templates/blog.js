import React from 'react'
import { graphql } from 'gatsby'

import Layout from '../components/Layout'

const Blog = (props) => {
    return (
        <Layout>
          
        </Layout>
    )
}

export default Blog